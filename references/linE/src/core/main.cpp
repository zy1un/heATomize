#include <SFML/Graphics.hpp>
#include "core/Game.hpp"


int main()
{
    // create the window
    Game game{LE::WINDOW_WIDTH,LE::WINDOW_HEIGHT,"LinEliminate"};
	game.run();

    // run the program as long as the window is open
    
}